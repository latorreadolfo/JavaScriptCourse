// SPLICE -> "arrayvar".splice(index, delete, elem1, elem2, elem3);

const names = ['Michael', 'Jim', 'Pam', 'Dwight', 'Kevin'];


// -> unshift
// names.splice(0, 0, 'Gabe');
// -> push
// names.splice(names.length, 0, 'Oscar');
// -> pop
// names.splice(-1, 1);

console.log(names);